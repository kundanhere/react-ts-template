import { LoginForm } from "@/components/login-form";

export default function LoginPage() {
  return (
    <div className="bg-background bg-dots flex h-screen w-screen items-center justify-center px-3">
      <LoginForm />
    </div>
  );
}
